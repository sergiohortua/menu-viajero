// @flow
import * as React from "react";
import "../assets/components/Header.css";

type Props = {};
const Header = (props: Props) => {
  return (
    <div className="App">
      <header className="App-header">EL VIAJERO</header>
    </div>
  );
};
export default Header;
